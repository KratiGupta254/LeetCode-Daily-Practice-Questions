class Solution {
    public boolean containsDuplicate(int[] nums) {
        Set<Integer> val=new HashSet<>();
        for (final int num : nums) {
            if (!val.add(num)) {
                return true;
            }
        }
        return false;
        
        
    }
}
